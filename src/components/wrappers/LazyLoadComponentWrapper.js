import React from 'react';
import { LazyLoadComponent } from 'react-lazy-load-image-component';

const LazyLoadComponentWrapper = () => {
    return (
        <LazyLoadComponent />
    );
}

export {
    LazyLoadComponent as default
}
